"""Placeholder for future ORM models.

Current persistence is SQLite table-oriented inside `app.trading` modules. Keep
this file so a future Render Background Worker + shared DB migration has a
stable import path.
"""
